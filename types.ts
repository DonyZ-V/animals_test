export interface Animal {
  id: string;
  name: string; // Chinese display name
  englishName: string; // English name for API context
  image: string; // URL to real image
  color: string; // Background/accent color
  sound: string; // Sound description
}